---
'@tapflowio/relay': patch
---

The comment panel beside a QA session says so when its comments cannot be loaded, instead of showing "No comments yet". It reads through the same query cache as the rest of the dashboard.
